<%= form_for(careplan) do |f| %>
  <% if careplan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(careplan.errors.count, "error") %> prohibited this careplan from being saved:</h2>

      <ul>
      <% careplan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <select id="careplan-client-picker">
  </select>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
